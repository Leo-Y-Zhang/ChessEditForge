{
  "name": "chess-edit-forge",
  "version": "0.1.0",
  "private": true,
  "description": "Cinematic vertical chess edits for TikTok - in-process Node renderer plus a self-contained browser player.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "build": "node build.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.2",
    "h264-mp4-encoder": "^1.0.12"
  }
}
